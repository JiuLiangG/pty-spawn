{
  "name": "pty-spawn",
  "version": "0.2.0",
  "description": "PTY-based bash execution for pi — makes isatty()=true, with headless terminal emulation",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "dependencies": {
    "node-pty": "^1.0.0",
    "@xterm/headless": "^5.5.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*",
    "vitest": "^3.0.0"
  }
}
